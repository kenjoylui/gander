Dragging to select text in a PDF follows your finger instead of freezing partway and then jumping, and dragging to the top or bottom of the screen scrolls the document, so a selection can run past the fold. Removing a granted folder asks first, since Android cannot hand a released permission back. Screen readers name the removal gesture, a burst of removals shows one badge rather than a dozen, and the line shown while a document is being built sits in the middle of the screen.
